// constants/api.js
// Centraliza a URL base da API para trabalhar bem em React Native.
// Altere o valor abaixo conforme seu ambiente (desenvolvimento/produção).

export const API_BASE_URL =
  // URL padrão para desenvolvimento local (backend mínimo incluido em /backend)
  'http://localhost:3000/api'

// Observação:
// - Em emuladores Android use 10.0.2.2 para acessar localhost do host.
// - Em emuladores iOS (simulador) use localhost normalmente.
// - Para produção, substitua pela URL pública do backend.
